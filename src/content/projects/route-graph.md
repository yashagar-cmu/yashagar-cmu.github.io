---
name: "Route Graph"
description: "Paste the output of netstat -rn from any machine (macOS, Linux, Windows) and read its routing table as a graph: interfaces, connected prefixes, next hops and ARP neighbors, with every flag decoded, a packet trace that shows the longest-prefix match and the two headers the frame carries, and a before/after diff that catches a VPN taking over. Runs entirely in the browser."
link: "https://yash-agarwal.org/tools/routes/"
tags: ["Networking", "15-441", "Visualization", "JavaScript"]
startDate: "2026-09-21"
---
